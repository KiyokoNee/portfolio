export const ContactForm = () => {
    const onSubmit = async (data: ContactFormData)=> {
        await fetch('/api/contact', {})
            .then()
            .catch()
    }
    return (
        <></>
    )
}