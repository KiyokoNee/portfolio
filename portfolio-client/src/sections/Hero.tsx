import {SectionWrapper} from "../components/SectionWrapper.tsx";

export const Hero = () => {
    return (
        <SectionWrapper>
            <h2 className="text-3xl font-bold mb-4">Hero</h2>
        </SectionWrapper>
    )
}